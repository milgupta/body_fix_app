import SwiftUI

struct StretchCardView: View {
    // TODO: Implement — individual stretch card component (name, reps, duration, description)

    var body: some View {
        Text("Stretch Card")
    }
}

#Preview {
    StretchCardView()
}
